# Dispatch Docker image build event to sui-operations repository.
name: Deepbook Sandbox images to Dockerhub
run-name: Publish deepbook-sandbox docker images for ${{ inputs.tag || github.ref_name }}

on:
  release:
    types: [published]
  workflow_dispatch:
    inputs:
      tag:
        description: "Release tag (e.g. v1.0.0). Defaults to the current branch/tag name."
        required: false

jobs:
  dispatch:
    name: Dispatch to sui-operations
    runs-on: ubuntu-latest
    permissions:
      contents: read
      actions: write
    steps:
      - name: Checkout repository
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # pin@v4.2.2
        with:
          ref: ${{ inputs.tag || github.ref_name }}
          fetch-depth: 1

      - name: Get latest commit SHA
        id: commit
        run: echo "sha=$(git rev-parse HEAD)" >> "$GITHUB_OUTPUT"

      - name: Dispatch deepbook-sandbox images to Dockerhub
        uses: peter-evans/repository-dispatch@ff45666b9427631e3450c54a1bcbee4d9ff4d7c0 # pin@v3.0.0
        with:
          repository: MystenLabs/sui-operations
          token: ${{ secrets.SUI_OPS_DISPATCH_TOKEN }}
          event-type: deepbook-sandbox-to-dockerhub
          client-payload: '{"sha": "${{ steps.commit.outputs.sha }}"}'
